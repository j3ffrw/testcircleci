checkout:
  post:
    - mvn dependency:copy-dependencies -DoutputDirectory=./ -gs settings.xml

machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t j3ffrw/places .
